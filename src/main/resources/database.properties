# Database Configuration

# H2 In-Memory Database (for development/testing)
# db.url=jdbc:h2:mem:healthapp;DB_CLOSE_DELAY=-1
# db.username=sa
# db.password=

# MySQL Database (for production)
db.url=jdbc:mysql://localhost:3306/sahacare?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver

# Connection Pool Settings
db.pool.initialSize=5
db.pool.maxActive=10
db.pool.maxIdle=5
db.pool.minIdle=2

# Alternative H2 Database Configuration (for development/testing)
# Uncomment these lines and comment out the MySQL configuration above to use H2
# db.url=jdbc:h2:mem:sahacare;MODE=MySQL
# db.username=sa
# db.password=
# db.driver=org.h2.Driver 